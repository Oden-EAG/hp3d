!> Purpose : extend prism layer for the truncation sphere
subroutine customize_geometry
  ! use pml
  ! use gmp
  ! implicit none

  ! if (PML_DOMAIN.lt.0) then
  !    call add_prism_layer(PML_SPHERE, 1, RPML_MAX - RPML_MIN, (NRDOMAIN+1))
  !    NRDOMAIN = NRDOMAIN + 1
  !    PML_DOMAIN = NRDOMAIN
  ! endif
  
end subroutine customize_geometry
